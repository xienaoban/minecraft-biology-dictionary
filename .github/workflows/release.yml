# Publish mod to Modrinth, CurseForge and GitHub
# Triggered by creating a GitHub release or pushing a tag

name: release

on:
  push:
    tags:
      - 'v*-mc*'  # Trigger on version tags like v0.7.0-mc1.20.1
  workflow_dispatch:  # Allow manual triggering from GitHub Actions page

jobs:
  prepare:
    runs-on: ubuntu-24.04
    if: startsWith(github.ref, 'refs/tags/')
    outputs:
      mod_version: ${{ steps.export.outputs.mod_version }}
      mc_version: ${{ steps.export.outputs.mc_version }}
      release_type: ${{ steps.export.outputs.release_type }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: check tag versions
        run: |
          chmod +x .github/scripts/check-tag-versions.sh
          .github/scripts/check-tag-versions.sh
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_ENV: ${{ github.env }}

      - name: export outputs
        id: export
        run: |
          echo "mod_version=${MOD_VERSION}" >> "$GITHUB_OUTPUT"
          echo "mc_version=${MC_VERSION}" >> "$GITHUB_OUTPUT"
          echo "release_type=${RELEASE_TYPE}" >> "$GITHUB_OUTPUT"

  build-fabric:
    needs: prepare
    runs-on: ubuntu-24.04
    outputs:
      mod_version: ${{ steps.export.outputs.mod_version }}
      mc_version: ${{ steps.export.outputs.mc_version }}
      release_type: ${{ steps.export.outputs.release_type }}
      mod_jar: ${{ steps.export-jar.outputs.mod_jar }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: setup jdk
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'microsoft'

      - name: make gradle wrapper executable
        run: chmod +x ./gradlew

      - name: export outputs
        id: export
        env:
          MOD_VERSION: ${{ needs.prepare.outputs.mod_version }}
          MC_VERSION: ${{ needs.prepare.outputs.mc_version }}
          RELEASE_TYPE: ${{ needs.prepare.outputs.release_type }}
        run: |
          chmod +x .github/scripts/export-outputs.sh
          .github/scripts/export-outputs.sh no-jar-name

      - name: build fabric
        run: ./gradlew fabric:build -Dbuild.release=true

      - name: test fabric
        run: ./gradlew fabric:testServer

      - name: rename fabric jar
        env:
          GITHUB_ENV: ${{ github.env }}
        run: |
          chmod +x .github/scripts/rename-jar.sh
          .github/scripts/rename-jar.sh fabric

      - name: export jar name
        id: export-jar
        run: |
          echo "mod_jar=$(basename "$MOD_JAR_FABRIC")" >> "$GITHUB_OUTPUT"
          echo "MOD_JAR=$(basename "$MOD_JAR_FABRIC")" >> "$GITHUB_ENV"

      - name: upload fabric jar
        uses: actions/upload-artifact@v4
        with:
          name: fabric-jar
          path: ${{ env.MOD_JAR_FABRIC }}
          retention-days: 1
          if-no-files-found: error

  build-forge:
    needs: prepare
    runs-on: ubuntu-24.04
    outputs:
      mod_version: ${{ steps.export.outputs.mod_version }}
      mc_version: ${{ steps.export.outputs.mc_version }}
      release_type: ${{ steps.export.outputs.release_type }}
      mod_jar: ${{ steps.export-jar.outputs.mod_jar }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: setup jdk
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'microsoft'

      - name: make gradle wrapper executable
        run: chmod +x ./gradlew

      - name: export outputs
        id: export
        env:
          MOD_VERSION: ${{ needs.prepare.outputs.mod_version }}
          MC_VERSION: ${{ needs.prepare.outputs.mc_version }}
          RELEASE_TYPE: ${{ needs.prepare.outputs.release_type }}
        run: |
          chmod +x .github/scripts/export-outputs.sh
          .github/scripts/export-outputs.sh no-jar-name

      - name: build forge
        run: ./gradlew forge:build -Dbuild.release=true

      # - name: test forge
      #   run: ./gradlew forge:testServer

      - name: rename forge jar
        env:
          GITHUB_ENV: ${{ github.env }}
        run: |
          chmod +x .github/scripts/rename-jar.sh
          .github/scripts/rename-jar.sh forge

      - name: export jar name
        id: export-jar
        run: |
          echo "mod_jar=$(basename "$MOD_JAR_FORGE")" >> "$GITHUB_OUTPUT"
          echo "MOD_JAR=$(basename "$MOD_JAR_FORGE")" >> "$GITHUB_ENV"

      - name: upload forge jar
        uses: actions/upload-artifact@v4
        with:
          name: forge-jar
          path: ${{ env.MOD_JAR_FORGE }}
          retention-days: 1
          if-no-files-found: error

  integration-test-fabric:
    needs: build-fabric
    runs-on: ubuntu-24.04
    outputs:
      mod_version: ${{ steps.export.outputs.mod_version }}
      mc_version: ${{ steps.export.outputs.mc_version }}
      release_type: ${{ steps.export.outputs.release_type }}
      mod_jar: ${{ steps.export.outputs.mod_jar }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: setup jdk
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'microsoft'

      - name: export outputs
        id: export
        env:
          MOD_VERSION: ${{ needs.build-fabric.outputs.mod_version }}
          MC_VERSION: ${{ needs.build-fabric.outputs.mc_version }}
          RELEASE_TYPE: ${{ needs.build-fabric.outputs.release_type }}
          MOD_JAR: ${{ needs.build-fabric.outputs.mod_jar }}
        run: |
          chmod +x .github/scripts/export-outputs.sh
          .github/scripts/export-outputs.sh

      - name: download fabric jar
        uses: actions/download-artifact@v4
        with:
          name: fabric-jar
          path: .

      - name: integration test fabric - run minecraft server
        run: |
          chmod +x .github/scripts/integration-test.sh
          .github/scripts/integration-test.sh
        env:
          LOADER_TYPE: fabric

  integration-test-forge:
    needs: build-forge
    runs-on: ubuntu-24.04
    outputs:
      mod_version: ${{ steps.export.outputs.mod_version }}
      mc_version: ${{ steps.export.outputs.mc_version }}
      release_type: ${{ steps.export.outputs.release_type }}
      mod_jar: ${{ steps.export.outputs.mod_jar }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: setup jdk
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'microsoft'

      - name: export outputs
        id: export
        env:
          MOD_VERSION: ${{ needs.build-forge.outputs.mod_version }}
          MC_VERSION: ${{ needs.build-forge.outputs.mc_version }}
          RELEASE_TYPE: ${{ needs.build-forge.outputs.release_type }}
          MOD_JAR: ${{ needs.build-forge.outputs.mod_jar }}
        run: |
          chmod +x .github/scripts/export-outputs.sh
          .github/scripts/export-outputs.sh

      - name: download forge jar
        uses: actions/download-artifact@v4
        with:
          name: forge-jar
          path: .

      - name: integration test forge - run minecraft server
        run: |
          chmod +x .github/scripts/integration-test.sh
          .github/scripts/integration-test.sh
        env:
          LOADER_TYPE: forge

  publish-fabric:
    needs: integration-test-fabric
    runs-on: ubuntu-24.04
    outputs:
      mod_version: ${{ steps.export.outputs.mod_version }}
      mc_version: ${{ steps.export.outputs.mc_version }}
      release_type: ${{ steps.export.outputs.release_type }}
      mod_jar: ${{ steps.export.outputs.mod_jar }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: export outputs
        id: export
        env:
          MOD_VERSION: ${{ needs.integration-test-fabric.outputs.mod_version }}
          MC_VERSION: ${{ needs.integration-test-fabric.outputs.mc_version }}
          RELEASE_TYPE: ${{ needs.integration-test-fabric.outputs.release_type }}
          MOD_JAR: ${{ needs.integration-test-fabric.outputs.mod_jar }}
        run: |
          chmod +x .github/scripts/export-outputs.sh
          .github/scripts/export-outputs.sh

      - name: download fabric jar
        uses: actions/download-artifact@v4
        with:
          name: fabric-jar
          path: .

      - name: publish fabric version to modrinth and curseforge
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: biology-dictionary
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}

          curseforge-id: 1426515
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}

          name: "Biology Dictionary ${{ github.ref_name }}-fabric"
          version: "${{ github.ref_name }}-fabric"
          version-type: ${{ env.RELEASE_TYPE }}
          changelog-file: CHANGELOG.md

          game-versions: ${{ env.MC_VERSION }}
          loaders: fabric
          java: |
            17

          files: ${{ env.MOD_JAR }}

          dependencies: |
            fabric-api
            cloth-config

  publish-forge:
    needs: integration-test-forge
    runs-on: ubuntu-24.04
    outputs:
      mod_version: ${{ steps.export.outputs.mod_version }}
      mc_version: ${{ steps.export.outputs.mc_version }}
      release_type: ${{ steps.export.outputs.release_type }}
      mod_jar: ${{ steps.export.outputs.mod_jar }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: export outputs
        id: export
        env:
          MOD_VERSION: ${{ needs.integration-test-forge.outputs.mod_version }}
          MC_VERSION: ${{ needs.integration-test-forge.outputs.mc_version }}
          RELEASE_TYPE: ${{ needs.integration-test-forge.outputs.release_type }}
          MOD_JAR: ${{ needs.integration-test-forge.outputs.mod_jar }}
        run: |
          chmod +x .github/scripts/export-outputs.sh
          .github/scripts/export-outputs.sh

      - name: download forge jar
        uses: actions/download-artifact@v4
        with:
          name: forge-jar
          path: .

      - name: publish forge version to modrinth and curseforge
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: biology-dictionary
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}

          curseforge-id: 1426515
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}

          name: "Biology Dictionary ${{ github.ref_name }}-forge"
          version: "${{ github.ref_name }}-forge"
          version-type: ${{ env.RELEASE_TYPE }}
          changelog-file: CHANGELOG.md

          game-versions: ${{ env.MC_VERSION }}
          loaders: forge
          java: |
            17

          files: ${{ env.MOD_JAR }}

          dependencies: |
            architectury-api
            cloth-config

  publish-github:
    needs: [publish-fabric, publish-forge]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: export outputs
        id: export
        env:
          MOD_VERSION: ${{ needs.publish-fabric.outputs.mod_version }}
          MC_VERSION: ${{ needs.publish-fabric.outputs.mc_version }}
          RELEASE_TYPE: ${{ needs.publish-fabric.outputs.release_type }}
          MOD_JAR_FABRIC: ${{ needs.publish-fabric.outputs.mod_jar }}
          MOD_JAR_FORGE: ${{ needs.publish-forge.outputs.mod_jar }}
        run: |
          chmod +x .github/scripts/export-outputs.sh
          .github/scripts/export-outputs.sh both-jars

      - name: download fabric jar
        uses: actions/download-artifact@v4
        with:
          name: fabric-jar
          path: .

      - name: download forge jar
        uses: actions/download-artifact@v4
        with:
          name: forge-jar
          path: .

      - name: publish fabric & forge version to github
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

          name: "Biology Dictionary ${{ github.ref_name }}"
          version: "${{ github.ref_name }}"
          version-type: ${{ env.RELEASE_TYPE }}
          changelog-file: CHANGELOG.md

          game-versions: ${{ env.MC_VERSION }}
          loaders: |
            fabric
            forge
          java: |
            17

          files: |
            ${{ env.MOD_JAR_FABRIC }}
            ${{ env.MOD_JAR_FORGE }}

          dependencies: |
            fabric-api
            architectury-api
            cloth-config
