# TODO

- Sync back to 1.21.11: keep tooltip width in `ScreenRenderingContext` instead of `Widget.TOOLTIP_WIDTH`, because `platform` is a reusable GUI support layer and must not depend on higher-level widget code.
